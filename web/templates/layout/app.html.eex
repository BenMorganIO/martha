<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Record meals and reflections with Martha.">
    <meta name="author" content="Ben A. Morgan">

    <title>Chat with Martha</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="row">
        <h2 class="col-xs-8"> Martha </h2>

        <div class="col-xs-4">
          <ul class="nav">
            <%= if logged_in?(@conn) do %>
              <li>
                <a> <%= current_user(@conn).name %> | <%= current_user(@conn).phone_number %> </a>
              </li>

              <li>
                <%= link "Logout", to: session_path(@conn, :delete), method: :delete %>
              </li>
            <% end %>
          </ul>
        </div>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
